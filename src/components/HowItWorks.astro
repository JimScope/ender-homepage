---
const steps = [
  {
    step: '01',
    title: 'Register your devices',
    description: 'Install our Android app on your phones or connect your modems. They become your personal SMS gateways.',
  },
  {
    step: '02',
    title: 'Get your API keys',
    description: 'Generate API keys from your dashboard. Use them to authenticate your applications.',
  },
  {
    step: '03',
    title: 'Send messages via API',
    description: 'Use our simple REST API to send SMS messages. Messages are routed through your registered devices.',
  },
  {
    step: '04',
    title: 'Track delivery',
    description: 'Monitor delivery status in real-time via webhooks or our dashboard. Get detailed analytics.',
  },
]
---

<section id="how-it-works" class="py-20 lg:py-32">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="text-center max-w-2xl mx-auto">
      <h2 class="text-3xl sm:text-4xl font-bold text-neutral-900">
        How it works
      </h2>
      <p class="mt-4 text-lg text-neutral-600">
        Get started in minutes with our simple setup process.
      </p>
    </div>

    <div class="mt-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {steps.map((item, index) => (
        <div class="relative">
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute top-8 left-full w-full h-0.5 bg-gradient-to-r from-primary-200 to-transparent -translate-x-4"></div>
          )}
          <div class="text-5xl font-bold text-primary-100">{item.step}</div>
          <h3 class="mt-4 text-lg font-semibold text-neutral-900">{item.title}</h3>
          <p class="mt-2 text-neutral-600">{item.description}</p>
        </div>
      ))}
    </div>

    <div class="mt-16 bg-neutral-900 rounded-2xl p-8 lg:p-12">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
        <div>
          <h3 class="text-2xl font-bold text-white">Simple API integration</h3>
          <p class="mt-4 text-neutral-400">
            Our REST API is designed for developers. Send your first message in just a few lines of code.
          </p>
        </div>
        <div class="bg-neutral-800 rounded-xl p-4 overflow-x-auto">
          <pre class="text-sm text-neutral-300"><code><span class="text-primary-400">curl</span> -X POST https://api.ender.dev/v1/sms \
  -H <span class="text-green-400">"Authorization: Bearer YOUR_API_KEY"</span> \
  -H <span class="text-green-400">"Content-Type: application/json"</span> \
  -d <span class="text-yellow-400">'{
    "to": "+1234567890",
    "message": "Hello from Ender!"
  }'</span></code></pre>
        </div>
      </div>
    </div>
  </div>
</section>
